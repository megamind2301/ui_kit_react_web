import React from "react";

const Button = (props) => {

    const {
        additionalClass
    } = props;

    return (
        <button>
            Button Name
        </button>
    )

}

export default Button;